# Святое Торжище — фронтенд

Клиентская часть магазина на Vue 3 + Vite.

Полная документация: [README в корне репозитория](../../README.md).

```bash
npm install
npm run dev
```

`VITE_API_URL` — URL бэкенда с `/api` (по умолчанию `http://localhost:3000/api`).
